# Perivale Warehouse — Process Map

Interactive HTML process map for the Perivale warehouse operation.

**Live demo:** https://adarshluxsh.github.io/warehouse-process-map/

The process map is a single self-contained `index.html` file — no build step, no
dependencies. To run it locally, just open `index.html` in a browser.
